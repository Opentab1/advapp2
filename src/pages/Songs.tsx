/**
 * Songs - Song log page
 * 
 * Wraps the existing SongLog component.
 * Keep it simple - just re-export with consistent naming.
 */

// Re-export the existing SongLog component
export { SongLog as Songs, SongLog as default } from './SongLog';
